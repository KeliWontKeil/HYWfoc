# L2 重构执行计划（定稿 v1.0）

> 状态：已评审通过，待执行
> 评审结论：以"恢复并明确 L2 内部分层（耦合域）"替代"纯函数化"；协议/调试层保持现状；内存布局不变；验收按"有感 + 无感已实现功能回归"。

## 一、重构目标（终稿）

| 目标 | 内容 | 强度 |
|---|---|---|
| G1 | **传参收窄**：消灭"同一对象双指针"（如 `Sensor_ReadEncoder(motor, &motor->sensor)`），函数只收其耦合域所需子结构体 + 必要标量 | 本次完成 |
| G2 | **结构体内聚**：私有状态类型下移至各 L2 模块 `.h`，共享类型留在精简后的 `foc_ctrl_types.h`，聚合体 `foc_motor_t` 上移 L1 | 本次完成 |
| G3 | **明确耦合域**：L2 内部分层（对应当初被文档漂移抹掉的分层意图）——Source / SourceMgr / 外环 / 电流环 / 执行输出 / 特殊状态六域；天然复杂耦合模块（SourceMgr、Executor）用"上下文视图"收敛 | 本次完成 |
| G4 | **空指针校验收敛**：删"全局静态实例不可能为 NULL"的检查，仅保留契约必要的 out 指针校验 | 本次完成 |
| G5 | **getter 清理**：删同模块内自产自销 getter，保留跨层只读窗 | 本次完成 |
| G6 | 协议/调试层保持现状（独立只读链，不改聚合访问） | 不动 |
| G7 | 算法与控制流程零修改 | 硬约束 |
| G8 | 内存布局不变（`foc_motor_t` 成员顺序不变） | 硬约束 |
| G9 | 范围仅核心库 `foc_core/`（含 L3，不含 L4 驱动） | 硬约束 |

## 二、六耦合域划分（G3）

数据流本质（重构后不变，仅显式化）：

```
sensor ─→ estimators(SMO/HFI/OpenLoop) ─→ source_mgr ─→ active_source_state + ctrl.electrical_angle
                                                            │
                                  outer_loop ─→ ctrl.iq_target
                                                            │
                                  current_loop ─→ ctrl.ud/uq/iq_measured
                                                            │
                                  actuation ─→ alpha_beta/applied_output/svpwm
```

| 域 | 文件 | 私有状态（现位于 `foc_ctrl_types.h`） | 跨域输入（显式传入） | 跨域输出 |
|---|---|---|---|---|
| Source | `foc_ctrl_estim_{smo,hfi}.c`、`foc_ctrl_openloop.c` | `estim_smo_state`、`estim_hfi_state`、`openloop_state` | `sensor`、`params`、`applied_output/ctrl`（电压）、`control_region` | 各源角度/速度 |
| SourceMgr | `foc_ctrl_source_mgr.c` | `source_mgr_state`、`source_switch_state` | 各 source 私有状态、`params`、`cfg`、`control_mode` | `active_source_state`、`ctrl.electrical_angle_rad`、外环/PID 同步、`encoder_services` |
| 外环 | `foc_ctrl_outer_loop.c` | `outer_loop` | `active_source_state`、`cfg`、`params.direction`、`control_region` | `ctrl.iq_target` |
| 电流环 | `foc_ctrl_current_loop.c` | `torque_current_pid`、`current_soft_switch_status` | `sensor`、`ctrl`、`params` | `ctrl.ud/uq/iq_measured` |
| 执行输出 | `foc_ctrl_actuation.c` | `svpwm_interp_state`、`alpha_beta`、`applied_output`、`phase_output_state` | `ctrl`、`params` | PWM 占空比 |
| 特殊状态 | `foc_ctrl_sens_{cogging_calib,reinit}.c` | `cogging_calib_state`、`reinit_state` | `sensor`、`ctrl`、`params` | `phase_output_state` |

关键总线：
- `foc_control_runtime_t` —— 控制链总线（executor→current_loop→actuation 全线传递）
- `foc_active_source_state_t` —— Source 总线（source_mgr→外环）

两总线以显式指针贯穿控制链。

## 三、类型归属重设计（G2）

依赖方向（无环）：

```
各 L2 模块 .h          定义私有类型 + 公开 API
        │ include（L2 允许依赖共享类型层）
精简后的 foc_ctrl_types.h  只保留共享类型
        │ include（L1 依赖 L2 类型层）
L1_Orchestration/foc_motor_aggregate.h  聚合 foc_motor_t（成员顺序不变）
```

### 3.1 类型归属清单

| 类型 | 现位置 | 新位置 |
|---|---|---|
| `foc_outer_loop_private_t` | `foc_ctrl_types.h` | `foc_ctrl_outer_loop.h` |
| `foc_estim_smo_state_t` | `foc_ctrl_types.h` | `foc_ctrl_estim_smo.h` |
| `foc_estim_hfi_state_t` | `foc_ctrl_types.h` | `foc_ctrl_estim_hfi.h` |
| `foc_openloop_state_t` + 状态宏 | `foc_ctrl_types.h` | `foc_ctrl_openloop.h` |
| `svpwm_interp_state_t` + 相关输出类型 | `foc_ctrl_types.h` | 执行输出域（`foc_ctrl_actuation.h` 或 SVPWM 侧） |
| `foc_reinit_state_t` + 阶段宏 | `foc_ctrl_types.h` | `foc_ctrl_sens_reinit.h` |
| `foc_cogging_calib_state_t` | `foc_ctrl_types.h` | `foc_ctrl_sens_cogging_calib.h` |
| `foc_current_soft_switch_status_t` | `foc_ctrl_types.h` | `foc_ctrl_current_loop.h` |
| `foc_source_mgr_state_t` / `foc_source_switch_state_t` | `foc_ctrl_types.h` | `foc_ctrl_source_mgr.h` |
| `foc_motor_t`（聚合体） | `foc_ctrl_types.h` | **上移** `L1_Orchestration/foc_motor_aggregate.h` |
| 共享类型（source 枚举、`active_source_state`、`control_runtime`、`phase_output_state`、`applied_output`、`params`、`cfg`、`pid`、`sensor_data_t` 等） | `foc_ctrl_types.h` | 保留在精简后的 `foc_ctrl_types.h` |

### 3.2 上下文视图（Executor / SourceMgr 摆脱 `foc_motor_t*`）

```c
/* foc_ctrl_source_mgr.h 内定义 */
typedef struct {
    foc_source_mgr_state_t      *mgr;
    foc_source_switch_state_t   *sw;
    foc_active_source_state_t   *active;
    sensor_data_t               *sensor;
    foc_estim_smo_state_t       *estim_smo;      /* 条件编译 */
    foc_openloop_state_t        *openloop;       /* 条件编译 */
    foc_outer_loop_private_t    *outer_loop;     /* 切换同步目标 */
    foc_pid_t                   *speed_pid;
    foc_pid_t                   *torque_pid;
    foc_control_runtime_t       *ctrl;
    foc_encoder_services_state_t*encoder_services;
    const foc_motor_params_t    *params;
    const foc_control_cfg_t     *cfg;
    /* 可再纳入 cogging_comp_status 等 */
} foc_source_mgr_ctx_t;
```

L1 从聚合体取各子结构地址构造视图传入。热路径为编译期地址赋值，无运行时开销。

## 四、传参收窄后的预期签名形态（G1 验收样例）

```c
/* L3 Sensor —— 消灭 motor/sensor 双指针 */
void Sensor_ReadEncoder(sensor_data_t *out, float dt_sec);
void Sensor_ReadCurrent(sensor_data_t *out);
void Sensor_ReadVBUS(sensor_data_t *out);

/* 外环 */
void FOC_SpeedOuterLoopStep(foc_outer_loop_private_t *state,
                            foc_pid_t *speed_pid,
                            foc_control_runtime_t *ctrl,
                            const foc_active_source_state_t *active,
                            const foc_control_cfg_t *cfg,
                            const foc_motor_params_t *params,
                            uint8_t control_region,
                            float speed_ref_rad_s, float dt_sec);

/* 电流环 */
void FOC_CurrentControlStep(foc_control_runtime_t *ctrl,
                            foc_pid_t *torque_pid,
                            foc_current_soft_switch_status_t *soft_switch,
                            const sensor_data_t *sensor,
                            const foc_motor_params_t *params,
                            float dt_sec);

/* SourceMgr —— 上下文视图 */
void FOC_SourceMgr_Select(foc_source_mgr_ctx_t *ctx);
void FOC_SourceMgr_Publish(foc_source_mgr_ctx_t *ctx);

/* 执行输出 */
void FOC_ControlApplyElectricalAngleRuntime(foc_control_runtime_t *ctrl,
                                            const foc_motor_params_t *params,
                                            svpwm_interp_state_t *svpwm,
                                            foc_applied_output_state_t *applied,
                                            foc_alpha_beta_phase_t *alpha_beta,
                                            float electrical_angle);
```

### 边界规则

- **G4 空指针**：签名收窄后 `if (motor == 0)` 自然消失；仅保留对可能为 NULL 的输出形参（`*mech_out`、`*elec_out`、`*out`）的校验。
- **G5 getter**：删模块内自用 getter（如 `Sensor_IsVBUSValid`）；**保留** `FOC_SourceMgr_ReadSourceSpeed/Angle`（跨源只读窗）与协议/调试层只读窗。
- **宏裁剪同步**：所有功能宏（`FOC_CURRENT_SOFT_SWITCH_ENABLE`、`FOC_ESTIMATOR_*_ENABLE`、`FOC_OPENLOOP_SOURCE_ENABLE`、`FOC_COGGING_*_ENABLE`、`FOC_REINIT_ENABLE`）裁剪分支内的签名必须同步修改——"声明/定义/调用三者一致"。

## 五、执行阶段与门禁

### 阶段 0 — 基线
- `--rebuild` 记录 0 error / warning 基线。
- 固化功能回归清单：
  - 有感：编码器外环（速度/角度）、电流环 PID、齿槽补偿、齿槽标定、重初始化
  - 无感：OpenLoop→SMO 升域/降域、SMO 收敛/发散、HFI（按实际启用的宏组合）

### 阶段 1 — 类型重归属（拆为 1a / 1b）
**结构性说明**：`foc_motor_t` 被所有 L2 模块引用，且 L2 禁止反向 include L1；因此 `foc_motor_t` 上移 L1 必须等传参收窄（阶段 2/3）完成后才能生效。故拆分：

- **阶段 1a（已完成）—— 私有类型下移**：无环依赖路线——
  1. `foc_ctrl_types.h` 彻底精简为**纯共享类型层**（移除全部私有状态类型、`svpwm_output_t/svpwm_interp_state_t`/`sensor_data_t` 至 L3 侧、`foc_motor_t` 聚合体）；
  2. 各 L2 模块 `.h` include 共享类型层 + `typedef struct foc_motor_t foc_motor_t;` 前置声明，并定义自己的私有状态类型 + 状态宏；`svpwm_output_t/svpwm_interp_state_t` 移入 `L3_Hal/foc_svpwm.h`、`sensor_data_t` 移入 `L3_Hal/foc_sensor.h`（修复 L3→L2 反向依赖）；
  3. 新建 `L2_Core/foc_motor_aggregate.h` 聚合 `foc_motor_t`（成员顺序不变）；
  4. 使用 `foc_motor_t` 完整类型的 `.c` 文件**首行** include `foc_motor_aggregate.h`（必须早于任何业务头，否则模块头 guard 抢先置位导致私有类型缺失）。
  - 依赖方向单行无环：`foc_ctrl_types.h ← 各模块.h ← foc_motor_aggregate.h`。
  - **验证结果**：`--rebuild` 0 error、0 新增 warning、代码尺寸与基线完全一致（Code=38608，证明零行为影响）。
- **阶段 1b（延后）—— `foc_motor_t` 上移 L1**：传参收窄完成后，将 `foc_motor_aggregate.h` 移至 `L1_Orchestration/`；L2 模块 API 已收窄不再依赖 `foc_motor_t`，移除各模块 `.h` 的前置声明。L2 自此不依赖 `foc_motor_t`。
- 同步更新 `builder.params` / `eide.yml`（新增/移动头文件目录或源文件清单）。

### 阶段 2 — 传参收窄（分域实施，每域编译验证）
- 建议顺序：`L3 Sensor → 电流环域 → 外环域 → OpenLoop 域 → 执行输出域 → 特殊状态域 → SourceMgr 域（最后）`。

### 阶段 3 — Executor / L1 适配 + 阶段 1b
- ISR 核心链、RunCycle、`FOC_App_ControlTrigger`、`FOC_App_OnPwmUpdateISR` 等改为构造上下文视图并分发。
- 完成阶段 1b：`foc_motor_t` 上移 `foc_motor_aggregate.h`，移除 `foc_ctrl_types.h` 过渡 include，L2 与 `foc_motor_t` 解耦。

### 阶段 4 — 清理
- 空指针校验收敛 + getter 清理。

### 阶段 5 — 文档同步
- `docs/architecture.md`（L2 六域划分、类型归属表、数据流图加"域边界"标注）
- `.clinerules`（类型文件划分描述对齐代码实际）
- `docs/development.md`、`CHANGELOG.md`、`NEXT_MISSION.md`、`AI_INITIALIZATION.md`
- 本文件保留为执行计划定稿

### 阶段 6 — 验收
- 0 error / 0 new warning + 全部回归点通过（有感 + 无感）。

## 六、执行顺序备注

- 新文件（如 `foc_motor_aggregate.h`）创建后必须同步 `builder.params`/`eide.yml` 的 include 路径。
- 每阶段结束必须编译验证，已有路径（有感/无感）不得退化。
- 禁止 git 提交，除非用户明确要求。