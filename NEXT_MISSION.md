# Next Mission

## 重要说明
- 本文件用于描述本次迭代的需求与约束，所有开发工作应以本文件为目标。
- 在开始编码前，请先阅读 `docs/engineering/dev-guidelines/rules/` 目录下对应语言（`cn/` 或 `en/`）的规则文件。
- 下一阶段默认不进行业务控制逻辑改动（不改FOC算法行为、不改协议行为），仅允许架构、组织与文档类变更。

## 版本
- **当前版本**：v0.4.0

## 参考文档
- 规则文件（核心）：`docs/engineering/dev-guidelines/rules/cn/`、`docs/engineering/dev-guidelines/rules/en/`
- 项目结构说明：`docs/README.md`
- 开发流程说明：`docs/development.md`

---

## 本次目标
v0.4.1 目标：在 v0.4.0 稳定基线上，完成“仅架构/组织/文档”治理迭代，为后续库化与开源拆分提供清晰边界与可维护流程。

### 优先级与阶段划分

执行顺序说明：先完成库边界与目录结构，再做文档体系治理。依赖前置未完成时，不推进后续文档收敛任务。

定位约束：单电机 FOC 驱动库，集成调试协议与错误处理；不考虑多电机实例扩展。

#### P1（库结构化优先）
P1.1 库边界与仓库结构落地
- 目标：将 L1-L3 + API 作为可移植核心包，其他工程文件作为特定平台示例。
- 交付：
	- 核心库目录与工程示例目录拆分方案（含文件迁移清单）。
	- 将 FOC 库文件独立出项目工程；将现有工程作为 `uvision_EIDE` 示例。
	- 示例工程目录预留为“硬件/软件”双目录：硬件目录后续补充，软件目录承载当前工程。
	- API 分为两类：
		- 核心侧提供空实现/接口约束文件（放在示例外）。
		- 各示例提供本平台 API 实现文件（放在示例内）。
	- L1-L3 作为公共依赖库，示例工程仅引用，不做拷贝副本。
- 确认方式：输出目录草案 + 迁移清单 + 依赖关系图（本阶段不设硬性验收条款）。

#### P2（开源基础合规）
P2.1 许可证与第三方依赖合规
- 目标：满足开源发布的法律与分发要求，使用 MIT 协议。
- 交付：
	- 项目许可证文件与版权声明。
	- 第三方组件清单与许可证说明（含 `Firmware` 目录依赖）。
- 确认方式：仓库内可追溯的许可证与第三方声明文件齐备。

P2.2 开源协作治理文件
- 目标：让外部贡献者可直接参与协作。
- 交付：
	- `CONTRIBUTING`、`CODE_OF_CONDUCT`、`SECURITY` 等基础文件。
	- Issue/PR 模板与最小审查流程说明。
- 确认方式：新贡献者可按文档完成一次标准提交流程（不要求发布演练）。

#### P3（文档体系治理，依赖 P1 完成后进行）
P3.1 分层边界静态审计与文档同步
- 目标：基于最终库结构复核 L1/L2/L3/L4 + Special Layer 依赖方向，消除边界歧义。
- 交付：
	- 依赖边界审计清单（违规项、风险等级、建议动作）。
	- 同步更新 `docs/architecture.md` 与 `docs/structure-and-dependency-tree.md`。
- 确认方式：审计清单与结构图一致即可，不单设流程型验收。

P3.2 SSOT 收敛与交叉引用规则
- 目标：协议、默认参数、调度频率、通道映射在各文档保持一致。
- 交付：
	- 文档一致性检查清单（版本号、默认参数、通信通道、调度频率、故障策略）。
	- `README.md`、`docs/*`、`CHANGELOG.md` 的交叉引用规则与执行说明。
- 确认方式：完成一致性清单并消除已知冲突项。

P3.3 工程目录职责与提交边界文档化
- 目标：明确“源码/构建产物/IDE工程文件/文档”边界，减少无关噪声变更。
- 交付：
	- 目录职责说明与提交流程边界约定。
	- 明确产物文件不进入版本发布提交的规则。
- 确认方式：规则文件与 `.gitignore`/发布流程描述一致。

P3.4 规则与流程文档标准化
- 目标：开发规则、发布流程、执行步骤保持一致，避免流程漂移。
- 交付：
	- 每次流程变化后同步更新中英文规则与开发文档。
	- 增加“待人工验证”标记机制，用于当前无法自动验收条目。
- 说明：移除“新人可仅凭文档完成完整构建与发布演练”作为本阶段硬验收项。

#### P4（库用户文档与集成指南）
P4.1 库快速接入与移植 FAQ
- 目标：面向库使用者，而非仅项目开发者。
- 交付：
	- 库快速接入文档（最小初始化、参数配置、调试协议启用）。
	- 常见故障排查与移植 FAQ。
- 确认方式：按文档可完成“编译 + 基本通信联通”最小闭环。

---

#### 备忘录项，暂时不进行开发
1. 槽极补偿（抗槽齿效应、抑制脉动转矩）
2. 算法微调（电流环参数整定与控制效果优化）
3. 控制逻辑与协议逻辑进一步隔离（结合后续库化阶段评估）
4. 无感电机控制（有感控制长期稳定后再规划）

## 备注（可选）
