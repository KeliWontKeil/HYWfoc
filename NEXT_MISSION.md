# 下一阶段任务（NEXT_MISSION）

## 0. 目标与边界

- 当前稳定基线：`v1.4.2`
- 下一目标版本：`v1.4.3`

## 1. 本阶段目标

### 1. 齿槽补偿效果验证与调优 ✅（v1.4.1 → v1.4.2）
  - 重构完成：从"初始化阶段自动标定"改为"运行时用户手动触发标定"模式。
  - 新机制：标定过程以 iq 计算值波动为输入，通过状态机在补偿执行步骤中驱动。
  - 初始化不再执行齿槽标定，仅输出齿槽表状态（已定义/空）。
  - 新增协议命令 `Y:G`（触发标定）和 `Y:D`（串口输出补偿表）。
  - 补偿仅在 `FOC_COGGING_COMP_ENABLE` 开启且齿槽表非空时生效。
  - **待验证**：实际标定效果需在硬件上验证并调整标定参数（步进电压、采集窗口等）。

### 2. 代码结构微调（v1.4.2 → v1.4.3）
- L2层控制链路传参简化：若可以只传结构体就不要逐字段传参。
- `MotorControlService_RunControlTask` 拆分为三个独立函数，替代 switch 组织结构。

## 3. 长期任务

1. 无感 FOC 算法实现。
2. 上位机实现（技术栈待定）。

## 其他
