# Next Mission

## 重要说明
- 本文件用于描述本次迭代的需求与约束，所有开发工作应以本文件为目标。
- 在开始编码前，请先阅读 `dev-guidelines/rules/` 目录下对应语言（`cn/` 或 `en/`）的规则文件。
- 当前分支：**framework**（用于新项目框架演示，内容简化）。

## 版本
- **当前版本**：v0.1.2

## 参考文档
- 规则文件（核心）：`dev-guidelines/rules/cn/`、`dev-guidelines/rules/en/`
- 项目结构说明：`docs/README.md`
- 开发流程说明：`docs/development.md`

---

## 本次目标
完成无刷电机驱动开发框架。

### 任务 1：✅ 已完成
加入传感器读取函数，读ADC和AS5600的值。它们的读取需要放在新文件中，并为每个数据增加简单的卡尔曼滤波器。请注意：读取函数将会放在1KHz任务中，所以注意不要过多的无用冗余语句。

### 任务 2：✅ 已完成
在1KHz任务中加入传感器读取函数，并在调试信息中输出定时器中断的1KHz任务运行时间。

### 任务 3：
实现FOC控制算法的基本框架，包括Clark变换、Park变换和SVPWM输出。需要创建新的算法模块来处理这些变换。

### 任务 4：
集成FOC算法到1KHz控制循环中，使用滤波后的传感器数据作为输入。
---

## 备注（可选）

