# 下一阶段任务（NEXT_MISSION）

## 0. 目标与边界

- 当前稳定基线：`v1.3.6`
- 下一目标版本：`v1.3.7`

## 1. 本阶段目标

### 1. 算法效果优化（高优先）

1. 齿槽补偿效果优化：在现有 `c24` 实现上继续优化低速补偿稳定性与可观测性。
2. 电流环与软切换策略复核：重点评估宏裁剪组合下的行为一致性。

### 2. 协议能力补齐（中优先）

1. 评估并实现齿槽参数链路（当前仅 `S:G` 状态链路已接入，参数子命令仍为预留符号）。
2. 完成 `c4` 主链与并行实现文档口径统一（并行实现不影响生产构建链）。

### 3. 文档与发布治理（持续）

1. 保持 `docs/architecture.md` 作为结构唯一事实源。
2. 所有接口/宏/默认值变化在同次迭代同步到协议与实例文档。

## 2. 已完成状态（供后续迭代参考）

1. 结构调整已完成：L2 生产主链固定为 `C1->C2->C3->C4->C5`。
2. L3 当前控制文件集稳定在 `C11/C12/C21/C22/C23/C24/C25/C31` 命名体系。
3. 配置头已收敛为 `foc_cfg_symbol_defs.h`、`foc_cfg_feature_switches.h`、`foc_cfg_init_values.h`、`foc_cfg_compile_limits.h`。

## 3. 长期任务

1. 无感 FOC 算法实现。
2. 上位机实现（技术栈待定）。
