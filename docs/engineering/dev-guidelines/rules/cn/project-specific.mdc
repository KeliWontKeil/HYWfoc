---
description: GD32F303CC FOC电机控制项目特定规则
globs: **/*.[ch]
alwaysApply: false
---

# GD32 FOC项目规则

## 代码风格与命名
- 函数：`Module_FunctionName`（例如：`Timer1_Init`、`PWM_SetDutyCycle`）
- 宏：`UPPERCASE_WITH_UNDERSCORES`
- 类型：`xxx_t`后缀
- 遵循现有模式，除非重构
- 上层（L1/L2/L3）接口命名应优先使用功能语义，不直接暴露外设名（如 `Usart1`、`Timer1`）。
- 对无法避免的跨层共用参数/回调/枚举类型，使用独立共用头文件集中定义，避免在API头文件中直接引用具体二级模块类型。

## 大括号规则 (Allman 风格)
- **大括号必须单独占一行**，不能与语句同一行
- 正确：```c
void Function(void)
{
    if (condition)
    {
        // code
    }
}
```
- 错误：```c
void Function(void) {
    if (condition) {
        // code
    }
}
```

## 模块边界
- 每个`Utilities/*`模块都是自包含的
- 通过头文件提供干净的API
- 避免跨模块全局变量
- 依赖分层采用：`LS` 配置层 → `L1` 运行编排层 → `L2` 服务/协议层 → `L3` 应用算法层 → `L41` 数学复用层 → `L42` 平台抽象层 → `L5` 板级驱动层
- `L1/L2/L3` 只能通过 `L42_PAL` 访问 `L5` 板级驱动
- `L5` 不能依赖 `L1/L2/L3`

## 头文件包含策略
- 非“自包含”模块应将模块依赖包含放在对应`.h`中，避免把主要依赖散落在`.c`中。
- `.c`文件优先只包含本模块头文件；仅当出现循环依赖或实现私有依赖时，才在`.c`中补充最小必要包含。
- 该规则的目标是让依赖结构在头文件层清晰可见，便于架构审计与迁移。
- L2/L3 公开头文件禁止直接包含 `Utilities/*` 头文件（必须通过特殊依赖层封装）。

## 优先级与文档来源
- **关键依据**：代码本身（`.c/.h`）应始终是最终事实
- 次级参考：`docs/` 目录下的说明文档
- 规则文件（`docs/engineering/dev-guidelines/rules/*`）用于指导 AI 和协作开发

## 可编辑范围
- ✅ `foc/src`、`foc/include`
- ✅ `examples/GD32F303_FOCExplore/software/Application/**`
- ✅ `examples/GD32F303_FOCExplore/software/Utilities/**`
- ✅ 文档：`docs/`, `NEXT_MISSION.md`, `CHANGELOG.md`
- ❌ 推荐不修改：`Firmware/**`、`.eide/**`（仅在必要时，如修复构建/烧录问题）

## 中断处理
- ISR 应保持精简，只做必要工作
- 在 ISR 中尽量避免阻塞操作；若必须阻塞，必须明确保证不会影响系统实时性
- ISR 应尽量转发到模块处理函数，避免直接包含复杂逻辑

## 时序与调度
- 主要节奏：`control_scheduler.c` 中由配置宏定义的任务槽
- 使用 DWT 计时工具监控关键路径的执行时间
- 定时器初始化应参数化（Timer1/Timer2/PWM）

## 执行时间与控制周期预算
- 任何控制任务都应确保最坏情况执行时间低于对应配置周期（预留裕量）
- 需要用 `ControlScheduler_EnableDWT()` / `ControlScheduler_GetExecutionCycles()` 进行检查
- 尽量避免在初始化/校准路径中使用阻塞延时（`delay_*` 等）
- 如果必须使用阻塞延时，应明确说明执行时间影响，并确保不会影响关键实时任务
## 安全性
- 访问指针前校验，不使用未初始化变量
- 需要 `NULL` 时必须包含 `<stddef.h>`
- 未经任务批准，避免修改时钟、内存映射和底层硬件配置

## 构建与烧录
- 开发环境主入口：VS Code + EIDE 扩展（编译/烧录/调试）
- 编译后端工具链：Keil μVision 5 + ARM Compiler 5 (AC5)
- 烧录/调试：ST-LINK（AI可按用户要求执行工具命令；硬件现象判定与最终验收由用户完成）
- 编译告警策略：禁止新增告警；若存在历史告警，需在文档中记录原因、处置与收敛计划

## 协议裁剪宏不得影响控制算法行为
- `FOC_PROTOCOL_ENABLE_*` 系列宏仅控制协议命令的可见性与参数读写通道，不得用于保护控制算法中的逻辑分支。
- 控制算法行为应仅由 `FOC_*_ENABLE` 系列宏（如 `FOC_CURRENT_SOFT_SWITCH_ENABLE`）裁剪。
- 协议裁剪宏若需影响算法默认值，应在 `foc_cfg_init_values.h` 中通过默认值间接实现，而非在 `motor_control_service.c` 或 `runtime_c4_runtime_core.c` 中用 `#if` 保护控制路径。

## 迭代与文档契约
- 只有在用户明确要求“进行下一次迭代”时，才认为是新迭代
- 每次迭代完成后需更新：
  - `CHANGELOG.md`（版本历史）
  - `NEXT_MISSION.md`（下一次目标）
  - `docs/` 中的相关说明
  - 规则文件（若流程/架构变化）

