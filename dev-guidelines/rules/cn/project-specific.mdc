---
description: GD32F303CC FOC电机控制项目特定规则
globs: **/*.[ch]
alwaysApply: false
---

# GD32 FOC项目规则

## 代码风格与命名
- 函数：`Module_FunctionName`（例如：`Timer1_Init`、`PWM_SetDutyCycle`）
- 宏：`UPPERCASE_WITH_UNDERSCORES`
- 类型：`xxx_t`后缀
- 遵循现有模式，除非重构

## 大括号规则 (K&R 风格)
- **大括号必须单独占一行**，不能与语句同一行
- 正确：```c
void Function(void)
{
    if (condition)
    {
        // code
    }
}
```
- 错误：```c
void Function(void) {
    if (condition) {
        // code
    }
}
```

## 模块边界
- 每个`Utilities/*`模块都是自包含的
- 通过头文件提供干净的API
- 避免跨模块全局变量

## 优先级与文档来源
- **关键依据**：代码本身（`.c/.h`）应始终是最终事实
- 次级参考：`docs/` 目录下的说明文档
- 规则文件（`dev-guidelines/rules/*`）用于指导 AI 和协作开发

## 可编辑范围
- ✅ `Application/Source`、`Application/Include`
- ✅ `Utilities/**`
- ✅ 文档：`docs/`, `NEXT_MISSION.md`, `CHANGELOG.md`
- ❌ 推荐不修改：`Firmware/**`、`.eide/**`（仅在必要时，如修复构建/烧录问题）

## 中断处理
- ISR 应保持精简，只做必要工作
- 在 ISR 中尽量避免阻塞操作；若必须阻塞，必须明确保证不会影响系统实时性
- ISR 应尽量转发到模块处理函数，避免直接包含复杂逻辑

## 时序与调度
- 主要节奏：`timer1_algorithm.c` 中的 1kHz/100Hz/10Hz/1Hz 任务槽
- 使用 DWT 计时工具监控关键路径的执行时间
- 定时器初始化应参数化（Timer1/Timer2/PWM）

## 执行时间与 1kHz 预算
- 任何 1kHz 任务都应确保最坏情况执行时间低于 1ms（预留周期）
- 需要用 `Timer1_EnableDWT()` / `Timer1_GetExecutionTime()` 进行检查- 尽量避免在初始化/校准路径中使用阻塞延时（`delay_1ms` 等）
- 如果必须使用阻塞延时，应明确说明执行时间影响，并确保不会影响关键实时任务
## 安全性
- 访问指针前校验，不使用未初始化变量
- 需要 `NULL` 时必须包含 `<stddef.h>`
- 未经任务批准，避免修改时钟、内存映射和底层硬件配置

## 构建与烧录
- 主工具链：Keil μVision 5 + ARM Compiler 5 (AC5)
- 开发环境：VS Code + EIDE 扩展用于编译
- 烧录/调试：ST-LINK（用户手动操作，AI不进行烧录或调试）
- 发现编译警告即应处理，多数警告反映潜在问题

## 迭代与文档契约
- 只有在用户明确要求“进行下一次迭代”时，才认为是新迭代
- 每次迭代完成后需更新：
  - `CHANGELOG.md`（版本历史）
  - `NEXT_MISSION.md`（下一次目标）
  - `docs/` 中的相关说明
  - 规则文件（若流程/架构变化）

