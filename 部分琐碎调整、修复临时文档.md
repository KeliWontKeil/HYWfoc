有感算法全部完成，以下问题需要修复调整，以进入v2.x.x开发阶段

# 2
- 宏/枚举/结构体定义调整
- 清理无用宏
- 可修改参数宏（运行时中储存的可修改的初值宏）和编译期参数宏（不可运行中修改的参数宏）命名需要有区别，可修改参数宏建议仿照COMMAND_MANAGER_DEFAULT_PID_ANGLE_KP加DEFAULT语义强调默认，表示是可后期修改的，而不可运行中修改的参数宏不能加该语义
- 宏头文件不聚合，非设置项的宏头去cfg命名语义
- 分类可能需要细化
- 所有宏/枚举/结构体定义全部分类放入LS层

# 3
## 结构精简及函数命名/变量命名修改/宏裁剪更新
- 该修复项问题不一定全部存在，需要进行评估
- 部分函数或代码命名不够直观，需要自解释性命名
- 部分地方存在大量冗余嵌套/冗余局部变量/冗余指针为空判断/冗余宏（可以直接调用传入结构体或参数却非要拷贝一份，可直接替换为表达式的位置却使用了一个只使用一次的临时变量储存计算值，明明在调用链前面已经保证了结构体不为空却非要又在里面判断一次，无用的兼容性宏重命名带来额外跳转降低代码可读性）
- 宏裁剪裁剪链不完整,可能存在宏裁剪没有遵循最大包覆原则，没有管入口点而是将入口点函数内部裁剪成一个空函数，或是未完全裁剪调用点/相关小型调用点导致空运行占用性能
- 宏裁剪没有包含结构体中的相关参数导致内存浪费

# 4
## 文档调整，注释删除/修改
- 部分注释可能存在错误，且有的地方写的太多了，不要写那么多注释，代码是有自解释性的
- 但是空API文件需要继续细化注释，它需要指导移植，所以需要详细化
- 更新所有文档