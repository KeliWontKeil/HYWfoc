有感算法全部完成，以下问题需要修复调整，以进入v2.x.x开发阶段
# 1
## 齿槽补偿代码结构优化及非算法问题调整
- 补偿算法和标定算法经过测试是正确的，请勿按照你以为的再做调整
- 每次补偿标定完后进入非正常欠压保护，软件错误复位多次才可以正常工作
- 补偿表宏初值定义，目前没法储存补偿表，在LS中新建一个.h文件，作为放置补偿表的位置，同时需要确认补偿表的初始化是否是正确的（假如定义了表并开启了宏）
- 结构/状态位置应符合规范,foc_control_c24_compensation.c中大量全局变量是否必要？删除不必要的，使用foc_motor_t结构体中的状态值进行替代，必要的相关状态变量应移入电机状态结构体中
- 补偿表很大，会占用堆栈空间、rom、ram，需要可宏裁剪

# 2
## 控制算法链结构修改
- motor_control_service.c中MotorControlService_RunControlTask函数会导致栈空间浪费，因为不同的模式会造成部分参数无用，于是浪费栈空间。应直接将其拆分成三种不同的算法函数。
- 在完成拆分后foc_app.h的FOC_App_RunControlAlgorithm函数也需要优化，它的内部的结构体定义似乎没有很大的存在必要。
- foc_app.c文件中大量volatile全局变量是否可以缩减

# 3
## 结构精简及函数命名/变量命名修改/宏裁剪更新
- 该修复项问题不一定全部存在，需要进行评估
- 部分函数或代码命名不够直观，请保持它们的自解释性
- 部分地方存在大量冗余嵌套/冗余局部变量（可以直接调用传入结构体或参数却非要拷贝一份，可直接替换为表达式的位置却使用了一个只使用一次的临时变量储存计算值）
- 宏裁剪裁剪链不完整,可能存在宏裁剪没有遵循最大包覆原则，没有管入口点而是将入口点函数内部裁剪成一个空函数，或是未完全裁剪调用点/相关小型调用点导致空运行占用性能
- 宏裁剪没有包含结构体中的相关参数导致内存浪费

# 4
## 文档调整，注释删除/修改
- 空API文件需要继续细化注释
- 齿槽补偿注释可能存在错误，且写的太多了，不要写那么多注释，代码是有自解释性的
- 更新所有文档